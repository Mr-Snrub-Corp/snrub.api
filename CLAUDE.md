# CLAUDE.md

Guidance for Claude Code (claude.ai/code) in this repo.

The single source of truth is `.cursor/rules/`. Read these first and follow them:

- `.cursor/rules/development.mdc` — communication, verify-first workflow, before-handover checks, process, tooling, testing
- `.cursor/rules/repository-structure.mdc` — stack, layered architecture, layer conventions, key patterns
- `.cursor/rules/commands.mdc` — dev / test / lint / migration commands and environment setup

Do not duplicate rule content here — update the rule files instead.
